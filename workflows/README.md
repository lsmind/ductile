# workflows

仓库级管线：`devcycle-start` / `devcycle-land`（说明见 [docs/DEVCYCLE.md](../docs/DEVCYCLE.md)）、`op-revise`（LLM 反思修订）、`dbcleanup`（真库清账：incidents triage/close + degraded 清旗 + 状态报告）。

`.hyper` 文件是 `devcycle` 系列的拓扑蓝本（`ductile hyper similar` 查重语料）。
