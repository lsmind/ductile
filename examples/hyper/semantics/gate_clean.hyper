HyperGraph("gate_clean")
  .require(judge=true)
  .vertex("src", role=source)
  .vertex("mid", role=default)
  .vertex("j", role=judge)
  .vertex("out", role=sink)
  .hedge("flow", kind=chain, src, mid, out)
  .hedge("q", kind=gate, judge=j, producers=mid, consumers=out)
  .deliver(out)
